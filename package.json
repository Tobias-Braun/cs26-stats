{
  "name": "cs26-stats",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "node scripts/build-elevation.mjs",
    "prebuild": "node scripts/build-elevation.mjs",
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "pnpm build && wrangler pages deploy dist"
  },
  "dependencies": {
    "@nuxtjs/supabase": "^1.4.6",
    "nuxt": "^3.16.2",
    "vue": "latest",
    "vue-router": "latest"
  },
  "devDependencies": {
    "@nuxt/devtools": "latest",
    "@cloudflare/workers-types": "latest",
    "typescript": "latest",
    "wrangler": "latest"
  }
}
